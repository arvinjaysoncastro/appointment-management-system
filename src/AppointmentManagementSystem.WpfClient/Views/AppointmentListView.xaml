<UserControl x:Class="AppointmentManagementSystem.WpfClient.Views.AppointmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" Height="40">
            <Button Command="{Binding LoadAppointmentsCommand}" 
                    Padding="10,5" 
                    Content="Refresh"/>
            <Button Command="{Binding AddAppointmentCommand}" 
                    Padding="10,5" 
                    Margin="10,0,0,0"
                    Content="Add Appointment"/>
        </StackPanel>

        <!-- Loading Indicator -->
        <TextBlock Grid.Row="1" 
                   Text="Loading..." 
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

        <!-- Appointments List -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Appointments}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Visibility="{Binding IsLoading, Converter={StaticResource InvertBoolToVisibilityConverter}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Patient Name" Binding="{Binding PatientName}" Width="*"/>
                <DataGridTextColumn Header="Appointment Time" Binding="{Binding AppointmentTime}" Width="200"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
